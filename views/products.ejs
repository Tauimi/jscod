
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h2><%= title %></h2>
    <ul>
        <% products.forEach(function(product) { %>
            <li><%= product.name %> - $<%= product.price %> <br> Manufacturer: <%= product.manufacturer %> <br> Description: <%= product.description %> <br> Specifications: <%= product.specifications %></li>
        <% }) %>
    </ul>
</body>
</html>
